{% extends 'base.html.twig' %}

{% block title %}Advertisement{% endblock %}

{% block body %}
    <h1>Advertisement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ advertisement.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ advertisement.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ advertisement.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ advertisement.price }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ advertisement.image }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ advertisement.city }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('advertisement_index') }}">back to list</a>

    <a href="{{ path('advertisement_edit', {'id': advertisement.id}) }}">edit</a>

    {{ include('advertisement/_delete_form.html.twig') }}
{% endblock %}